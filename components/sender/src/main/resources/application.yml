spring:
  application:
    name: sender
  cloud:
    stream:
      defaultBinder: kafka
      default:
        destination: json-file
        producer:
          content-type: application/json

      kafka:
        binder:
          brokers: localhost:9092
          defaultBrokerPort: 9092
